<%= form_with model:[@photo, @comment] do |form| %>
<h2>Add your comments</h2>
<div>
  <%= form.label "body" %>
  <%= form.text_field :body %>
</div>
     <div class='btn'>
      <%=form.submit "Send comment", class:'btn btn-lg btn-primary'%>
    </div>
</div>
<% end %>